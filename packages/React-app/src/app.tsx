import React from "react";

type AppProps = Record<string, unknown>;

const Appexported = () => {
  // const [magic, setMagic] = React.useState(true);
  return (
    <div>
      <h1>Good Morning Folks</h1>
      <button>see</button>
    </div>
  );
};

export default Appexported;
